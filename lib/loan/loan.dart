import 'package:flutter/material.dart';

class NewLoanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('New Loan Screen'),
    );
  }
}